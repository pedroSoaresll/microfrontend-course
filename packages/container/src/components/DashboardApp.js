import React from "react";
import { component } from "dashboard/DashboardApp";
import MicroFrontend from "./MicroFrontend";

export default () => {
  return <MicroFrontend component={component} />;
};
